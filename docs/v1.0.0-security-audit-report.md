# DNS Manager v1.0.0 — 安全审计与代码质量报告

> 生成日期：2026-05-05  
> 审计范围：全项目（Rust 后端 + TypeScript 前端 + .gitignore）

---

## 一、.gitignore 审查与完善

### 1.1 审计发现

| 文件 | 原状态 | 风险 |
|------|--------|------|
| `dns-manager/.gitignore` | 仅包含 Node.js / 编辑器忽略规则 | 缺少 Rust/Tauri/Windows 构建产物和密钥文件规则 |
| `dns-manager/src-tauri/.gitignore` | 仅 `/target/` 和 `/gen/schemas` | 缺少 Rust 备份文件和调试符号 |

### 1.2 已应用修复

**根 `.gitignore` 新增：**

| 类别 | 新增规则 |
|------|----------|
| 环境变量 | `.env`, `.env.*`, `!.env.example` |
| TypeScript | `*.tsbuildinfo` |
| Windows | `Thumbs.db`, `Desktop.ini`, `$RECYCLE.BIN/` |
| Rust/Cargo | `*.rs.bk`, `*.pdb` |
| Tauri | `src-tauri/target/`, `src-tauri/gen/schemas` |
| macOS | `.DS_Store`, `.AppleDouble`, `.LSOverride` |
| Linux | `.directory`, `*.desktop` |

**`src-tauri/.gitignore` 新增：**

- `*.rs.bk` — Rust 备份文件
- `*.pdb` — Windows 调试符号
- `.rustc_info.json` — 编译器信息缓存
- `.DS_Store` — macOS 系统文件

### 1.3 密钥泄漏审查

| 检查项 | 结果 |
|--------|------|
| `.env` 文件 | ✅ 未发现 |
| 硬编码 API 密钥 | ✅ 未发现（`"test-token-123"` 仅存在于 `#[cfg(test)]` 测试模块） |
| `credentials.json` | ✅ 存储在 `%APPDATA%/.dns-manager/`（项目外） |
| `tauri.conf.json` | ✅ 无敏感信息，CSP 配置合理 |
| `src-tauri/target/` 跟踪 | ✅ 被 `.gitignore` 正确忽略 |

---

## 二、Rust 后端修复

### 2.1 安全修复（Debug 泄漏）

| 文件 | 行号 | 问题 | 修复方式 |
|------|------|------|----------|
| `src-tauri/src/models/credential.rs` | 46 | `CredentialSecretData` 派生 `Debug`，通过 `{:?}` 可打印 API Token/SecretKey 等敏感字段 | 删除派生宏，手动实现 `Debug`，使用 `finish_non_exhaustive()` 掩盖所有字段 |
| `src-tauri/src/models/credential.rs` | 34 | `CredentialInput` 派生 `Debug`，前端提交的密钥参数可被打印 | 同上，只暴露 `provider_type` 和 `label` |

### 2.2 功能性缺陷修复（DNSPod Provider）

| 文件 | 行号 | 严重度 | 问题 | 修复 |
|------|------|--------|------|------|
| `src-tauri/src/providers/dnspod.rs` | 384 | 🔴 阻断 | `update_record` 中 `DomainId` 硬编码为 `0u64`，API 调用永远失败 | 改用传入的 `domain_id` 参数 |
| `src-tauri/src/providers/dnspod.rs` | 428 | 🔴 阻断 | `delete_record` 请求体缺少必填字段 `DomainId` | 添加 `"DomainId"` 到 JSON body |
| `src-tauri/src/providers/dnspod.rs` | 440 | 🔴 阻断 | `search_records` 请求体缺少必填字段 `DomainId` | 添加 `"DomainId"` 到 JSON body |
| `src-tauri/src/providers/dnspod.rs` | 183 | 🔴 可靠性 | HTTP 状态码赋给未使用的 `_status`，API 返回 HTTP 错误时被 JSON 反序列化掩盖 | 添加 401/403 状态码检查，提前返回 `Unauthorized` 错误 |

### 2.3 可靠性修复（Panic 消除）

| 文件 | 行号 | 问题 | 修复 |
|------|------|------|------|
| `src-tauri/src/providers/dnspod.rs` | 93 | `reqwest::Client::builder().build().expect()` — HTTP 客户端创建失败会 panic 终止应用 | 返回 `Result<Self, ProviderError>` |
| `src-tauri/src/providers/dnspod.rs` | 140 | `HmacSha256::new_from_slice(key).expect()` — HMAC 初始化失败会 panic | 内联函数 `hmac_sha256` 改为返回 `Result`，错误向上传播 |
| `src-tauri/src/providers/dnspod.rs` | 167 | `serde_json::to_string(&body).unwrap_or_default()` — 序列化失败静默返回空字符串 | 改为 `?` 传播错误 |
| `src-tauri/src/providers/cloudflare.rs` | 71 | `reqwest::Client::builder().build().expect()` — 同上 | 返回 `Result<Self, ProviderError>` |
| `src-tauri/src/providers/alidns.rs` | 110 | `reqwest::Client::builder().build().expect()` — 同上 | 返回 `Result<Self, ProviderError>` |
| `src-tauri/src/providers/alidns.rs` | 135 | `HmacSha1::new_from_slice(key.as_bytes()).expect()` — 同上 | 返回 `Result`，`sign`/`build_url` 及全部 7 处调用点同步更新 |
| `src-tauri/src/providers/mod.rs` | 79-93 | 工厂函数 `create_provider` 未处理 Provider 构造函数新返回的 `Result` | 3 处 `::new()` 调用添加 `?` |
| `src-tauri/src/error.rs` | 103 | `ProviderError` 缺少通用错误变体 | 添加 `Other(String)` 变体 |

### 2.4 可靠性修复（错误静默丢弃）

| 文件 | 行号 | 问题 | 修复 |
|------|------|------|------|
| `src-tauri/src/commands/shared.rs` | 8 | `fs::create_dir_all(&path).ok()` — 权限/磁盘满错误被静默丢弃 | 添加 `log::error!` |
| `src-tauri/src/commands/shared.rs` | 16 | `serde_json::from_str(&contents).unwrap_or_default()` — JSON 损坏时静默返回空 Vec，已存凭据全部丢失 | 添加 `log::error!` |
| `src-tauri/src/commands/shared.rs` | 24 | `fs::write(&path, json).ok()` — 写入失败静默丢弃 | 添加 `log::error!`，并单独处理序列化失败 |

---

## 三、TypeScript 前端修复

### 3.1 类型安全

| 文件 | 问题 | 修复 |
|------|------|------|
| `src/types/index.ts:21` | `RecordType` 联合类型缺少 9 个 Rust 后端支持的变体（CERT, DNSKEY, DS, LOC, NAPTR, SMIMEA, SSHFP, TLSA, URI） | 补全全部 19 种 DNS 记录类型 |
| `src/routes/DomainDetail.tsx:17` | `RECORD_TYPES` 常量列表只有 8 种类型（缺少 SOA, PTR 和其他高级类型），用户无法通过 UI 创建这些记录 | 更新为完整 19 种类型 |

### 3.2 错误处理

| 文件 | 问题 | 修复 |
|------|------|------|
| `src/routes/Credentials.tsx` | 无 error 状态变量；加载失败用 `console.error` 静默吞错；保存/删除失败用 `alert()` 弹窗（阻塞 UI、不与设计系统一致） | 添加 `error` state + `ErrorAlert` 组件（支持 dismiss）；保存/删除失败改为设置 error 状态 |
| `src/routes/Search.tsx` | 无 error 状态变量；搜索失败用 `console.error` 静默吞错，结果被设为空数组，用户无法区分"无结果"和"出错" | 添加 `error` state + `ErrorAlert` 组件 |
| `src/components/common/ErrorAlert.tsx` | 不支持手动关闭错误 | 添加 `onDismiss` prop + Dismiss 按钮 |

### 3.3 可靠性

| 文件 | 问题 | 修复 |
|------|------|------|
| `src/routes/DomainDetail.tsx:50` | `useEffect` 无竞态条件防护（`provider`/`domainId` 快速切换时，旧请求的结果可能覆盖新请求） | 添加 `ignore` flag clean-up 模式 |

---

## 四、构建验证

| 步骤 | 状态 |
|------|------|
| TypeScript 类型检查 (`npx tsc --noEmit`) | ✅ 通过 |
| Vite 构建 (`npm run build`) | ✅ 通过 |
| Rust 编译 (`cargo check`) | ✅ 通过（用户终端验证） |

---

## 五、Git 推送就绪状态

- ✅ 无敏感文件（`.env`, 密钥文件等）
- ✅ `.gitignore` 覆盖所有构建产物和系统文件
- ✅ 构建产物 `dist/`, `src-tauri/target/` 被正确忽略
- ✅ `Cargo.lock` 作为未跟踪文件，建议在推送时提交（Tauri 应用应锁定依赖版本）

### 提交建议

```bash
cd dns-manager
git add -A
git commit -m "v1.0.0: Security audit — fix credential Debug leak, DNSPod API bugs, error handling, .gitignore hardening"
```

---

## 六、已知遗留事项（非阻塞）

| 类别 | 描述 | 优先级 |
|------|------|--------|
| Rust | `shared.rs` 中 `app_data_dir()` 仅支持 Windows，macOS/Linux 回退到 `.` — 推荐使用 Tauri 的 `app.path().app_data_dir()` | 低 |
| Rust | `CloudflareProvider::list_records` 内额外调用 `get_domain` 仅为了获取域名 — 增加了 API 调用量 | 低 |
| Rust | `dnspod.rs` 中 `parse_record_type` 对未知类型静默回退到 `RecordType::A` — 应添加 `log::warn!` | 低 |
| Rust | `retry.rs` 重试日志中尝试计数偏移一位 ("Attempt 2/3" 实际是第一次重试) | 低 |
| TS | `Sidebar.tsx` / `Settings.tsx` 中版本号硬编码为 `"0.1.0"` | 低 |
| TS | `Credentials.tsx:101` 嵌套三元表达式在切换 provider 时仅重置第一个凭据字段 | 低 |
| 无障碍 | icon-only 按钮缺少 `aria-label`，对话框缺少 `role="dialog"` | 低 |

---

*本报告由 Qoder 自动生成，覆盖安全审查、功能性缺陷修复、错误处理加固三个维度的完整变更记录。*
